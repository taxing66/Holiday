<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.joiway.devin.holiday">

    <application
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.DemoActivity_1"/>
        <activity android:name=".controller.activity.DemoActivity_2"/>
        <activity android:name=".controller.activity.TestActivity"/>
        <activity android:name=".activity.XUtilsActivity"/>
        <activity android:name=".activity.ChateActivity">
        </activity>
    </application>

</manifest>